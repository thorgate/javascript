# @thorgate/eslint-config

## Installation

Install the package
```shell script
yarn install @thorgate/eslint-config
```

Install required plugins
```shell script
yarn install @thorgate/eslint-config
```
