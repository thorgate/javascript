{
  "useWorkspaces": true,
  "npmClient": "yarn",
  "packages": [
    "packages/*",
    "examples/*"
  ],
  "version": "independent",
  "command": {
    "publish": {
      "message": "chore(release): publish %s",
      "ignoreChanges": [
        "*.md"
      ]
    }
  }
}
